<ContentDialog
    x:Class="GyroShell.Controls.RestartDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GyroShell.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Restart GyroShell"
    PrimaryButtonText="Restart now"
    PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonText="Restart later"
    CornerRadius="8"
    CloseButtonStyle="{ThemeResource DefaultButtonStyle}">

    <Grid>
        <TextBlock Text="GyroShell needs to restart to apply this setting. You can either choose to restart now, or restart later. Changes will not be applied until you restart GyroShell." TextWrapping="WrapWholeWords"/>
    </Grid>
</ContentDialog>
